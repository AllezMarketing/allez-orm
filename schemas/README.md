This folder is intentionally kept in the repo, but its **contents are generated** by the AllezORM CLI/Studio
and are ignored by .gitignore. Add your own schemas locally; they wont be committed unless you override ignores.
